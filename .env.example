# ── Backend ──────────────────────────────────────────────────
# Local development (default: file:./data.db)
DB_PATH=./data.db

# Turso cloud (Vercel production) — get from https://turso.tech
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# HTTP server port (local Node.js only)
PORT=3000

# Allowed origin for CORS (set to frontend URL in production)
# e.g. https://your-frontend.vercel.app
FRONTEND_ORIGIN=http://localhost:5173

# ── Frontend ─────────────────────────────────────────────────
# Leave empty to use relative /api proxy (same origin)
# Set to backend URL when deployed separately, e.g.:
# VITE_API_BASE_URL=https://your-backend.vercel.app
VITE_API_BASE_URL=
